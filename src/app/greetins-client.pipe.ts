// import { Pipe, PipeTransform } from '@angular/core';

// @Pipe({
//   name: 'greetinsClient',
//   standalone: true
// })
// export class GreetinsClientPipe implements PipeTransform {

//   transform(value: User, ...args: unknown[]): string {
//     return `Olá ${value.firstName} ${value.lastName}`;
//   }

// }

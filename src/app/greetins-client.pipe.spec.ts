import { GreetinsClientPipe } from './greetins-client.pipe';

describe('GreetinsClientPipe', () => {
  it('create an instance', () => {
    const pipe = new GreetinsClientPipe();
    expect(pipe).toBeTruthy();
  });
});
